API_TOKEN = "{{ bot_api_token }}"
WEBHOOK_HOST = "{{ server_dns_name }}"
WEBHOOK_LISTEN = "127.0.0.1"
WEBHOOK_LISTEN_PORT = {{ webhook_listen_port }}

WEBHOOK_URL_BASE = "https://%s" % WEBHOOK_HOST
WEBHOOK_URL_PATH = "/webhook/%s/" % API_TOKEN
